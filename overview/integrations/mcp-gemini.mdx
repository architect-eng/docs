---
title: "Connect MCP with Gemini CLI"
description: "Set up Architect's MCP server with Google's Gemini CLI to read docs and call the Customer API"
sidebarTitle: "Gemini CLI"
---

## Prerequisites

- [Gemini CLI](https://github.com/google-gemini/gemini-cli) installed
- An Architect account with access to a workspace

## Setup

<Steps>
  <Step title="Open your Gemini CLI settings">
    Gemini CLI stores MCP server configuration in a `settings.json` file. Open it at:

    - **macOS/Linux:** `~/.gemini/settings.json`
    - **Windows:** `%USERPROFILE%\.gemini\settings.json`

    Create the file if it doesn't exist.
  </Step>

  <Step title="Add the Architect MCP server">
    Add the following to your `settings.json`:

    ```json
    {
      "mcpServers": {
        "architect": {
          "url": "https://mcp.dev.tryarchitect.com/mcp"
        }
      }
    }
    ```

    <Note>
    If you already have other MCP servers configured, add `"architect"` as a new key inside the existing `mcpServers` object.
    </Note>
  </Step>

  <Step title="Restart Gemini CLI">
    Close and reopen Gemini CLI for the new configuration to take effect. You can verify the server is connected by running `/mcp` in the CLI.
  </Step>

  <Step title="Authenticate with Architect">
    The first time you use an Architect tool, Gemini CLI will open a browser window asking you to sign in to your Architect account. Grant the requested permissions and your session will be saved.
  </Step>

  <Step title="Start using Architect tools">
    You can now ask Gemini to interact with your Architect workspace:

    - *"Use architect_docs to show me the available API endpoints"*
    - *"List my pages using the Architect API"*
    - *"Look up how to create a prospect, then create one for jane@acme.com"*
  </Step>
</Steps>

## Available tools

Once connected, Gemini CLI has access to two Architect MCP tools:

| Tool | Description |
| --- | --- |
| `architect_docs` | Read Architect MCP docs plus targeted Customer API reference slices before you make a request. |
| `architect_api` | Call Customer API endpoints for the authenticated tenant after you know the exact method, path, and payload. |

The recommended workflow is: use `architect_docs` first to understand what's available, then use `architect_api` to execute.

## Troubleshooting

<AccordionGroup>
  <Accordion title="Gemini CLI doesn't see Architect tools">
    Run `/mcp` to check if the Architect server is listed. If not, verify your `settings.json` file is saved correctly and restart Gemini CLI.
  </Accordion>

  <Accordion title="Connection times out">
    Make sure the URL is exactly `https://mcp.dev.tryarchitect.com/mcp`. Gemini CLI supports Streamable HTTP transport natively, so no bridge is needed.
  </Accordion>

  <Accordion title="Authentication window doesn't appear">
    Make sure your default browser isn't blocking pop-ups. Try opening `https://mcp.dev.tryarchitect.com/mcp` directly in your browser to verify the server is reachable.
  </Accordion>
</AccordionGroup>
